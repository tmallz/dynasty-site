export interface TrendingPlayerPageDto {
	playerName?: string;
	playerId?: string;
	playerPosition?: string;
	playerTeam?: string;
	playerImageUrl?: string;
	timesWaived?: number; // added or dropped
}
